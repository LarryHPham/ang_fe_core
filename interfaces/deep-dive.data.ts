export interface VideoStackData{
    id: string,
    timeStamp: number,
    keyword: string,
    title: string,
    teaser?: string,
    videoThumbnail: any,
    videoUrl?: string,
    thumbnailHeight?: string,
    thumbnailWidth?: string
}
